---
description: Publish the application (Build)
---

# Publish Workflow

This workflow builds the application for production.

## Steps

1. **Build**: Run the build command to compile TypeScript and bundle assets.

    ```bash
    npm run build
    ```

2. **Verify**: Check the `dist` directory (if applicable) or verify the build output for errors.

// turbo-all
